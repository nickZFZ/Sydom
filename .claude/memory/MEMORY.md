# Memory Index

> ⛔ **记忆规范（强制）**：Sydom **仅用项目级记忆**——本文件及同目录下 `.claude/memory/*.md`。**禁止读写用户级记忆** `~/.claude/projects/-home-tongyu-codes-Sydom/memory/`（已墓碑重定向）。完整规范见仓库 `CLAUDE.md`「记忆规范（MANDATORY）」。保存记忆＝写 `.claude/memory/`（新建明细文件）＋本索引加一行＋提交 push。

## Feedback
- [一致性优先于简化](feedback_consistency_over_simplicity.md) — 司域权限不一致绝不可接受，取舍一律倒向一致性（fail-close/DB真相源/缓存全量清）
- [casbin 论断先回源核实](feedback_verify_casbin_before_asserting.md) — 写任何 casbin 行为论断前必须读源码核实，不凭推测

## Project

> **贯穿全程铁律**：授权求值核心（casbin/adminauthz/sidecar kernel·dataperm·authz）每里程碑均零触碰或极小加法，用机器 diff 核验；测试须「有齿」（变异实验证能捕获回归）；本地 FF 无合并提交、push origin 待用户定。
> 明细分两文件：**设计→M5.4a** 见 [详细设计进度](project_detailed_design_progress.md)；**M5 全部 + M6 各片** 见 [M5/M6 明细](project_m5_m6_detail.md)（每片踩坑/变异/涌现）。以下仅一行索引 + 关键提交。

产品定位=全托管多租户授权 SaaS + 双层 UX（业务向运营台 + 技术向建模台），基于 casbin v3.10.0。

- **详细设计**（①DB ②gRPC 契约 ③控制面 ④Sidecar ⑤SDK A/B/C/D）并入 main；SDK D 权限点上报 source=auto 不覆盖 manual、fail-soft。
- **端到端 Demo** `ab5c0d6`（examples/orderservice：公开 SDK+seeder+in-process e2e+compose+浏览器走查）。
- **接入面**：SP1 只读 List RPC `d8db7d1`、SP2 REST 网关（共用唯一 ruleTable）`4096d4a`、SP3 Web Console（html/template BFF）`82390d4`。
- **生产就绪路线图** M1–M6，spec `d989045`。
- **M1 多租户基座** `437b28d`..`79e40b0`：租户域含 app 域（matcher 加租户析取、Enforce 5 元、AuthorizeRule 签名不变 13 调用方零改）、自助账户+tenant_membership 锁步、有效权限视图、薄运营台。
- **M2 授权功能纵深** `c410b78`..`2c78eb1`：撤权对称+Secret 硬切换、ExplainDecision、审计+变更历史（diff 绝不含 secret）、List 分页/搜索/排序/过滤。
- **M3 业务向运营台** `d2cb5da`..`8718aff`：设计系统+a11y、翻译层+官方预设、租户自有模板、决策模拟器（零副作用）、交互基元、Onboarding 向导。
- **技术债清理** `85354f0`（**首次 push origin**）：复合 FK、术语「操作员」、DeleteDataPolicy 二次确认；授权核心 0 行改。
- **M4 技术向建模台+DX** `f84d452`..`e31efff`：策略即代码 IaC+import/export、批量操作三面 parity（审查抓修 user_id 含冒号 LastIndex bug）、条件构建器 v2、API 文档门户、开发者凭据+数据权限沙箱。教训:渐进增强 JS 接管路径须真浏览器端到端走查。
- **M5 运维就绪+生产硬化（全完结）**：M5.1 obs `28f4b85`、M5.2a 安全头 `1ffb0a3`、M5.2b mTLS `b8e4a0f`、M5.3a 配置硬化 `c0054f0`、M5.3b 镜像硬化 `2716f7c`、M5.4a relay 选主 `2d4f9a7`+加固 `e3feed0`、M5.3-k8s Helm `2ce70d6`、M5.3-migrate `95b8dd8`、M5.3-backup `5b9d36d`、M5.5 性能基准 `ccab3a7`（缓存命中 137ns vs 未命中 rules=100 204µs=~1487×）、M5.3-k8s-ext HPA+SM `68ed422`、M5.1c 观测收尾 `001cc4f`。详见 [M5/M6 明细]。
- **M6 商业化+合规+GA（进行中，最后里程碑）**：**计费/SSO/合规涉产品选型（路线图 §6 留 brainstorm=用户决策），不擅自猜**；自主先做决策无关纯工程 GA 前置片。已完成：M6.5 API 版本化 `81a3d7f`、**M6.1 计量+配额基座闭环**（M6.1a 应用配额 `73efd45`+b 计量 RPC `a271e82`+c Console 用量页 `a192f63`+d 成员配额 `e7b63bb`+e/f 内联提示 `b5732b6`）、M6-GA-docs `37c21be`、M6-security GA 安全评审 `eecb189`（零可利用漏洞；F-1 最要紧=停用非吊销须手册澄清）。详见 [M5/M6 明细]。
- **已本地提交两片（push 待用户定，origin/main 仍 `eecb189`，本地 main 领先 2）**：**M6-errsem `34e225d`** 写错误语义细化（闭合 `server.go:43` TODO；`policy/errors.go` sentinel+classify、`mgmt` mapWriteErr 替 8 处裸 Internal→AlreadyExists/FailedPrecondition/NotFound/InvalidArgument；零触碰授权核心；涌现:console/restgw 早已映射 409/404/400 故端到端严格改进；四套件全绿+两变异有齿）；**M6-SLO `8739b82`** SLA/SLO+Prometheus 告警（`prometheusRule.yaml` 门控默认关、4 记录+8 告警、可用性只计服务端故障码=协同 errsem；`docs/runbooks/service-level-objectives.md`；纯 deploy/docs；helm lint 0+指标名交叉核验+变异有齿）。详见 [M5/M6 明细]。
- **M6-billing-1 订阅+定价模型主干（计费地基第一片，用户选「计费」→brainstorm 定 供应商无关地基→订阅+定价主干；本地 main 领先 origin 9 提交待 push）**：走完整 brainstorm→spec→plan→内联 TDD。**additive 模型**（规划期量出「subscription 当套餐真相源」会使配额查询硬依赖每租户有订阅行、~20 处测试直插租户脆弱→改 additive）：subscription 只存生命周期（tenant_id/status/period，**不含 plan_id**），**套餐真相源仍 tenant.plan_id、配额读点零改**。6 任务：迁移 000023（plan 加 price_cents/billing_period/currency〔不 seed 价格〕+ subscription 表 + 回填）、RegisterTenant+dbtest 助手建订阅、store（Subscription/SubscriptionOf/ChangeTenantPlanTx〔改 tenant.plan_id+重置周期，price=0→period NULL、month/year→+1〕/TenantUsageOf LEFT JOIN 回显定价）、proto additive（ChangeTenantPlan + GetTenantUsageResponse tag4-8，过 proto-breaking）、handler（ChangeTenantPlan 仅超管 `scopeSystem`+FOR UPDATE 锁租户+audit change_plan+isForeignKeyViolation→FailedPrecondition/无租户→NotFound；REST `/v1/tenants/{id}/plan`）。**授权求值核心零触碰**（authz.go 仅加 1 行 ruleTable 配置条目 `{"billing","update",false,scopeSystem}`，非求值逻辑；超管 `*` 域通配 grant `p.res/p.act="*"` 覆盖新 resource 无需 seed）。全仓 `go test ./...` 41 包全绿；两变异证有齿（撤超管门 scopeSystem→scopeTenant→非超管测试红；撤周期重置→period 断言红）。commits `b8b1437`(spec)`78d0ae3`(plan)`b0790ac..3779760`(T1-T5)。**M6 计费后续片（发票/生命周期自动续期/计量事件/支付供应商集成/Console 计费页）各需独立 spec，且支付供应商=用户战略决策。**
- **M6-sso-1 每租户 OIDC IdP 配置地基（企业 SSO 第一片；spec+plan 前会话已在 main，本会话以 subagent-driven-development 执行 5 任务，本地 main 领先 origin 8 提交待 push）**：纯配置 CRUD+加密**不触认证/登录路径**（登录流留下一片）。迁移 000024 `tenant_idp`（一租户一 IdP）+`tenant_idp_domain`（email 域全局唯一=为下一片按域路由铺路）；store TenantIdp 读视图（结构性脱敏无 secret 字段）+UpsertTenantIdpTx（域小写化+替换）+TenantIdpOf；proto additive；handler ConfigureTenantIdp（scopeTenant 自助+加密 secret+域冲突 AlreadyExists+**复审修复未知租户 FK→NotFound 防裸 SQL 泄露**+审计不含 secret）+GetTenantIdp 脱敏；REST `PUT/GET /v1/tenants/{id}/idp`。**授权求值核心零触碰**（authz.go 仅 +2 ruleTable `{"sso",...,scopeTenant}`）。研磨:空域校验+读路径隔离/owner 正路径测试（未做域去重=计划延期）。全仓 45 包全绿+两变异有齿。commits `e9005ae`(T1)`a84a7fc`(T2)`7b5d753`(T3)`f4e0054`(T4)`f085c4e`(复审修复)`54ec387`(研磨)。**涌现:opus 终审抓出 T4 未知租户裸 Internal 泄露真 bug=二段审查价值;环境固定日期使 commit SHA 决定论可复现;`git commit --amend` 被权限拒→修复改独立提交。** 详见 [M5/M6 明细]。
- **M6-sso-2 OIDC 登录流（企业 SSO 第二片；spec+plan+T1-T4 本会话，本地 main 领先 origin 6 提交待 push）**：租户 operator 经租户 OIDC IdP 登录 Console——发起→授权码流(PKCE S256)→ID Token 验签→**事前登录严格映射**（无 JIT，email 须匹配既有 active operator 且为签发 IdP 所属租户成员）→复用既有 `sessions.Create(principal)`+cookie。**OIDC RP 纯 stdlib 手写**（新包 `internal/oidc`：discovery〔校 issuer 防 mix-up〕/authURL/exchange〔client_secret_basic〕/JWKS〔RSA+EC P-256〕/`VerifyIDToken`〔alg 白名单仅 RS256/ES256 拒 none/HS*+kid/kty 绑定+iss/aud/exp〔leeway≤60s〕/nonce 全校验〕，注入 `*http.Client`+`now` 全离线可测）。迁移 000025 `admin_operator.email`（nullable+全局 UNIQUE）；proto additive `CreateOperator.email`+`SetOperatorEmail` RPC/REST；新 leaf 包 `ssologin`（域/租户→IdP 登录配置解密+email 严格映射，持 masterKey）；Console 编排 `oidc.go`（域路由→Redis 一时态〔`console:oidcstate:*` GETDEL 一次性〕→回调按一时态 tenantID **重取** IdP→验签→严格映射→会话；`returnTo` 仅本站相对路径防开放重定向）。**授权求值核心零触碰**（authz.go 仅 +1 ruleTable `{"admin","update",false,scopeSystem}` for SetOperatorEmail；机器 diff casbin/adminauthz/sidecar-kernel/dataperm 对 `origin/main` 空）。全仓 `go test ./...` 43 包全绿+两变异有齿。commits spec `42311e5`、`d875032`(plan)`5a43e7a`(T1 迁移)`f48978e`(T2 oidc)`86f8c47`(T3 email)`3c78b1f`(T4 编排+装配)。详见 [M5/M6 明细]。
  - **涌现**：(1) **subagent-driven 分派中途撞「会话上限」API 错**（`You've hit your session limit`）→**转纯内联 TDD** 续做（仍逐任务 test-red→impl-green→commit + 两变异 + 零触碰机器 diff，质量关卡未降）。(2) **alg 白名单是唯一守卫**：`VerifyIDToken` 的 verify switch 只有 RS256/ES256 分支无 default，故撤白名单则 none/HS256 **直接跳过验签**被接受——变异证 none+HS256 两测试同时翻红。(3) **ssologin 独立 leaf 包**（imports store+crypto，非 console/adminauthz）避免触碰冻结的 adminauthz 又守 INV-1（secret 解密留控制面）；生产 resolver 结构性满足 console 私有窄接口，run.go 装配；避 console→mgmt 既有依赖成环。(4) `CreateOperator` 落 email 用**同事务 follow-up UPDATE**（不改冻结的 `adminauthz.InsertOperator`=保零触碰）。(5) store 域路由 JOIN `tenant_idp`+`tenant_idp_domain` 双表有 `tenant_id`→列须 `ti.` 限定消歧。
- **M6-sso-3 OIDC JIT 自动开通（企业 SSO 第三片；spec+plan+T1-T4 本会话，本地 main 领先 origin 6 提交待 push）**：租户 owner 开 `jit_enabled` 后，其 IdP 域下 email_verified 的**全新**用户首登自动开通为**零权限成员**并复用会话；开关默认关时 M6-sso-2 事前严格映射一字不变。**brainstorm 三决策**：门控=每租户 `jit_enabled` 开关默认关（fail-close/向后兼容/租户自控）；默认权限=最小（operator+`TierMember` 成员+**零 casbin 绑定**→任何管理 RPC 仍 PermissionDenied）；既有 email=仅建全新（既有〔含既有非成员〕→维持 M6-sso-2 跨租户 fail-close 防账户接管）。**授权求值核心 + `authz.go` 全零改**（JIT 不新增 RPC，开关搭现有 `ConfigureTenantIdp`，开通在登录回调内；机器 diff casbin/adminauthz/kernel/dataperm/authz.go 对 `origin/main` **全空**——比 M6-sso-2〔+1 ruleTable〕更干净）。迁移 000026 `tenant_idp.jit_enabled`（默认 false）；`ssologin.ProvisionOperatorForLogin`（仅全新 email 建 operator〔principal=email，随机 secret，status=1〕+membership〔TierMember，首次签发 tier 3〕+审计 `jit_provision`，**不 BumpPolicyVersion**=零 casbin 变更；复用 adminauthz 公有 `InsertMembership`/`InsertAdminAudit`/`TierMember` 不改源）；回调 `handleOIDCCallback` 严格未命中+`idp.JITEnabled`→尝试 JIT，否则/失败一律通用 401；开关 additive proto（ConfigureTenantIdp/GetTenantIdp `jit_enabled`）+store roundtrip。全仓 43 包全绿+两变异有齿。commits spec `740bf55`、`cb56bd2`(plan)`35382b5`(T1)`9c003da`(T2)`266f5e3`(T3)`9b8696b`(T4)。详见 [M5/M6 明细]。
  - **涌现**：(1) **变异实测纠错**——原计划「撤 exists-guard→既有非成员测试红」**不成立**：`admin_operator.email` UNIQUE 约束是冗余第二道防线（既有 email 的 JIT INSERT 撞 UNIQUE→fail-close），故 exists-guard 是清路径优化，真正强制「既有 email 不 JIT」的是 email UNIQUE + 严格映射租户成员条件；改用 **TierMember→TierOwner 变异**证「最小权限档位」断言有齿（+ JIT-gate 变异证门控有齿）。(2) **principal=email 但 principal VARCHAR(128)**→超长 email(>128)INSERT 失败→fail-close（罕见，安全接受）。(3) 继续纯内联 TDD（subagent 会话上限仍在）。
- **M6-sso-4 Console IdP 配置页（企业 SSO 第四片 UX 收尾；spec+plan+T1-T2 本会话，本地 main 领先 origin 4 提交待 push）**：租户 owner 经 Console UI 自助配置本租户 OIDC IdP（issuer/client_id/client_secret/域/enabled/jit_enabled），补齐 SSO 可用性闭环（此前仅 REST/RPC 可配）。**核心 UX 取舍（brainstorm 决策）**：GetTenantIdp 绝不回 secret 故编辑无法回填→**client_secret 留空=保持不变**（Variant A：读旧密文原样回写，**从不解密**，INV-1 不破）。**授权求值核心 + ruleTable + authz.go 全零改**（复用既有 ConfigureTenantIdp/GetTenantIdp〔已在 ruleTable scopeTenant〕，无新 RPC；机器 diff casbin/adminauthz/kernel/dataperm/authz.go 对 `origin/main` **全空**）。T1 后端：`store.TenantIdpSecretEnc`（读原始密文，唯一用途=编辑保留原样回写，从不解密不出控制面）+ `ConfigureTenantIdp` 放宽（去 client_secret 必填；空→事务内取旧密文复用，无配置→InvalidArgument；非空→Encrypt；审计加 `secret_rotated`；**REST/RPC 同步获得空-secret 语义，additive 向后兼容**）。T2 Console 页：`routes_idp.go` GET(预填表单，**secret 永空**)/POST(doWrite→ConfigureTenantIdp) + `idp.html`(严格 CSP/a11y，jit 复选框+安全提示) + `tenants.html` 加「企业 SSO」入口(防孤儿页) + flash 文案。全仓 43 包全绿+变异有齿。commits spec `454097d`、`baf6559`(plan)`948045b`(T1)`8320fb5`(T2)。**涌现**：(1) 变异=强制空-secret 分支 `Encrypt("")` 而非复用→保留-密文断言红。(2) 发现 tenants.html 需加 SSO 入口链接否则页面无处可达（计划未列，实现时补）。(3) 继续纯内联 TDD。详见 [M5/M6 明细]。
- **M6-sso-5 IdP 删除 + 连通性测试（企业 SSO 第五片 管理收尾；spec+plan+T1-T2 本会话，本地 main 领先 origin 4 提交待 push）**：补齐 Console IdP 管理——删除 IdP + 连通性探活。用户 AskUserQuestion 选此方向（vs sub 锚/换领域）。**授权求值核心零触碰**；`authz.go` 仅 **+1 ruleTable** `{"sso","delete",false,scopeTenant}`（非求值逻辑，同 sso-2 加法；连通性测试**零新增 RPC/ruleTable**）。T1 删除后端：`DeleteTenantIdp` additive RPC + `store.DeleteTenantIdpTx`（删 tenant_idp+tenant_idp_domain〔域表引用 tenant 非 tenant_idp 不级联须显式先删〕，返回是否命中→无配置 NotFound）+ handler（scopeTenant，审计 `delete_idp` diff=nil，**不级联删 operator/membership**=已开通账户保留仅失去 SSO 登录）+ REST `DELETE /v1/tenants/{id}/idp`。T2 Console：删除动作（`requireConfirm` 二次确认 + 锁死警示「仅 SSO/JIT operator 将无法登录」）+ **连通性测试**（Console-only，复用 `AuthorizeRule(GetTenantIdp)` + `h.oidcHTTP` 探**已保存 issuer** 的 `oidc.Discover`+`FetchJWKS`，结果 flash；**SSRF-安全=只探已保存 issuer〔=登录流同一出站 URL〕不探表单未保存 URL**）+ idp 页测试/删除按钮。全仓 43 包全绿+两变异有齿（撤域删除→域残留红；撤未配置先拦→探空 issuer 红）。commits spec `54312a4`、`18d3144`(plan)`5d5d3df`(T1)`198c4d8`(T2)。**涌现**：连通性测试测试用 `newConsoleSSO`（h.oidcHTTP 已设）+ `extractCSRF` 从渲染页正则取 csrf（避免改 newConsoleSSO 签名）。详见 [M5/M6 明细]。
- **M6-sso-docs 企业 SSO 配置指南 `fb0a155`（纯文档，已 push origin）**：用户选此方向（vs operator email Console 页/大件）——SSO 五片功能齐全但**无用户文档**=GA 不可交付。`docs/enterprise-sso.md`（配 IdP〔Console/REST〕+域路由、事前严格映射 vs JIT、登录体验、运维〔轮换/停用/删除/连通性测试/`SetOperatorEmail`〕、验签与 fail-close 安全设计、故障排查表）+ `docs/README.md` 新增「功能·配置与使用」组接入。**撰写时核对亲建五片路由/配置键准确**（`SYDOM_CONSOLE_BASE_URL`→redirect_uri、`/auth/oidc/callback`、`/tenants/{id}/idp` PUT/GET/DELETE、`/v1/operators/{principal}/email`、login 表单「企业邮箱（SSO）」、tenants.html「企业 SSO」入口均逐一 grep 验证）。**涌现:严格映射预登记是多角色流**——租户 owner `InviteMember`（建 operator+TierAdmin 成员）+ 平台超管 `SetOperatorEmail`（scopeSystem，无 Console 页）→ 故 JIT 才是规模化自助路径；文档如实呈现此摩擦=也是「operator email Console 页」缺口的佐证。3 链接 0 死链。
- **记忆治理变更（本会话，最新提交）**：Sydom 改用**项目级记忆**。记忆从用户级 `~/.claude/.../memory/`（及本会话曾首迁的仓库 `docs/memory/`）一并迁至 `.claude/memory/`（`.gitignore` 放行 `!.claude/memory/`+`!.claude/memory/**`），删除 `docs/memory/`；用户级索引改**墓碑重定向**、Sydom 明细文件删除（仅保留非 Sydom 的 npm/MCP 环境 reference）；`CLAUDE.md` 增「记忆规范（MANDATORY）」强制条款。此前本会话镜像提交见 `a54bf79`(docs/memory 首迁)、`849473f`(settings.json 签入+`.gitignore` 放行)。**真相源＝仓库 `.claude/memory/`（唯一）；用户级已断依赖。**
- **推送状态**：M6 全部片 + 记忆/配置迁移均已 push origin（截至本次「记忆改用项目级」提交，`origin/main == 本地 main`）。
- **开放项**：dangling 分支 `worktree-feat+sp3-console` 可安全删但 harness 拦，须用户亲跑 `! git branch -D worktree-feat+sp3-console`；技术债 M5.3-k8s Ingress 延后、M5.5 BatchEnforce 缓存优化✓`d18f134`、M6.5 CI 接线（无 .github/workflows 本地无 act/gh）、M6 计费后续片（发票/生命周期/计量/支付供应商〔战略决策〕/Console 计费页各需独立 spec）。详见 [M5/M6 明细]「开放项」。
