# IDE
.idea/
*.iml
.vscode/

# Claude Code (ignore all of .claude/ except the shared, checked-in project settings
# and the project-level memory — Sydom uses project memory only, see CLAUDE.md)
.claude/*
!.claude/settings.json
!.claude/memory/
!.claude/memory/**

# CodeGraph index (local code-intelligence, not in repo)
.codegraph/

# OS
.DS_Store
Thumbs.db

# Java
target/
*.class
*.jar
*.war
*.ear
*.log

# Node
node_modules/
dist/
.env
.env.local

# Build
build/
out/

# Browser-validation artifacts (kept locally, not in repo)
/*.png
.playwright-mcp/
/casbin/

# superpowers visual-companion brainstorm prototypes
.superpowers/
