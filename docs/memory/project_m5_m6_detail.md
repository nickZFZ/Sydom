---
name: project-m5-m6-detail
description: Sydom M5（运维就绪+生产硬化）与 M6（商业化+合规+GA）各子片的完整明细、踩坑、变异实验、涌现——MEMORY.md 索引对应节的详情
metadata: 
  node_type: memory
  type: project
  originSessionId: 5fbb0648-e2a6-4e14-bad3-50b6b1108b2c
  modified: 2026-07-19T11:38:50.332Z
---

MEMORY.md 的 M5/M6 一行索引在此展开。铁律见 [[project-detailed-design-progress]] 与 MEMORY.md 顶部（授权核心零触碰、测试有齿、本地 FF push 待用户定）。

## M5 运维就绪+生产硬化（M5.1–M5.5 全完结，路线图 M1–M6 仅剩 M6）

- **M5.1 obs 基座 `28f4b85`**：internal/obs 自持 Registry+10 低基数指标+/metrics+结构化日志+request_id；kernel 唯一改动=NewBoundedCache 导出。
- **M5.2a 安全响应头 `1ffb0a3`**：严格 CSP 无 unsafe-inline、internal/secheaders、从源清 6 内联 style；审查修 javascript: 取消被 CSP 拦。
- **M5.2b policysync mTLS `b8e4a0f`**：派生式 MutualServer/Client、现有 Server/Client 零改、无 CN↔app_id 耦合；教训:跨 TLS 版本握手断言须实测。
- **M5.3a 配置硬化+生产 fail-close `c0054f0`**：internal/deploycfg、environment 解析拒静默降级、`_FILE` 密钥、生产缺 TLS 拒启。
- **M5.3b 容器镜像硬化 `2716f7c`**：4 Dockerfile distroless static:nonroot uid 65532、两层 digest 固定、gcr 经 DaoCloud 代理绕墙；教训:distroless 切换须审计容器内工具依赖。
- **M5.4a 控制面多副本安全（relay 选主）`2d4f9a7`+加固 `e3feed0`**：leader 包 `pg_try_advisory_lock` 专用 `*sql.Conn` 持会话锁+健康检查+显式解锁 failover；run.go 接线 onElected set/clear `sydom_relay_leader` gauge+传 lctx 停 drain；relay.go 逐字不改；生产 footprint 恰 3 文件。两变异（撤门→重复投递 10、去 FOR UPDATE→丢更新 v7/20）证有齿；opus 终审修 1 Important 测试假绿（failover 测试须记真实赢家再杀）。加固落实终审两 Minor：leader 重新参选加退避防 onElected 快返回 busy-loop 打爆 PG〔变异撤退避→1944 次 FAIL〕+release unlock 加 5s 超时上界。已 push origin。
- **M5.3-k8s Helm chart+sidecar 参考 `2ce70d6`**（纯 deploy/* 零 Go）：`deploy/helm/sydom-controlplane/` chart——Deployment `replicas:2`+distroless nonroot uid 65532/readOnlyRoot/drop ALL+httpGet `/healthz`·`/readyz`（补 distroless 无 wget 缺口）+`SYDOM_*_FILE` subPath 挂密钥不进 env+config.yaml 含 DSN 落 Secret 非 ConfigMap+`existingSecret` 门控+生产 TLS fail-close+PDB+SA 不挂 token+Prometheus 注解；`deploy/k8s/sidecar-reference.yaml`。M53K-1..7 PASS。已 push origin。
- **M5.3-migrate 迁移自动化+零停机 `95b8dd8`**（触碰 Go 非授权核心）：`db/migrations/embed.go` `//go:embed *.sql`+`internal/db.RunMigrationsFS`（golang-migrate iofs，幂等）+控制面 `-migrate` 模式（轻量 `LoadDSN` 只 file `database_dsn`+`SYDOM_DATABASE_DSN` 覆盖，迁移无关密钥/TLS 不走生产 fail-close→runMigrate→退出）+Helm `pre-install,pre-upgrade` hook Job（`migrations.enabled` 门控、复用硬化镜像、hook-weight -5、失败则 release 失败=fail-close）+`docs/runbooks/zero-downtime-migrations.md`（expand/contract 纪律、`maxUnavailable:0`）。变异空 FS 报错证有齿。已 push origin。
- **M5.3-backup 备份恢复 `5b9d36d`（M5.3 收官）**（纯 deploy/scripts+helm+runbook 零 Go）：PG=唯一真相源、Redis=瞬态可重建故不备份。`pg-backup.sh`（`pg_dump -Fc`+`RETENTION_DAYS` prune+`POST_HOOK`）+`pg-restore.sh`（`pg_restore --clean --if-exists`+须 `--yes` 防误删退 2）+Helm `backup-cronjob.yaml`（`backup.enabled` 默认 off、postgres 镜像 nonroot、DSN secretKeyRef、缺 pvcClaim/dsnSecret fail）+`docs/runbooks/backup-restore.md`（两层=逻辑 pg_dump 司域交付+PITR 委托托管 PG）。真实往返 seed→backup→drop→restore→值 42 回来 OK。已 push origin。
- **M5.5 授权决策性能基准 `ccab3a7`（M5 收官）**：数据面热路径（gRPC Check→`Authorizer.Check`→`kernel.Engine.Enforce` casbin `SyncedCachedEnforcer`+LRU1024）立 Go benchmark，放独立包 `test/bench` 经公开 API 构建种子=内核字节不变。`docs/runbooks/performance-baselines.md`。实测（Ryzen 9 7900X/Go1.26.3）：缓存命中 **137ns/op**（8 allocs）vs 未命中 rules=100 **203,768ns**（3455 allocs）=**~1487×**；未命中随 rules 近线性 23µs→204µs→2.3ms（O(rules)）。已 push origin。
- **M5.3-k8s-ext HPA+ServiceMonitor `68ed422`**（纯 deploy/helm 零 Go；本地 main 未 push origin）：HPA（`autoscaling/v2` CPU 扩缩，门控 `autoscaling.enabled` 默认关，min2/max5/80；Deployment `replicas` 条件化 `{{if not .Values.autoscaling.enabled}}` 避与 HPA 打架）+ServiceMonitor（`monitoring.coreos.com/v1` 抓 /metrics，门控默认关；`service.yaml` 加 `metrics` 端口 targetPort health）。Ingress 延后（gRPC controller 选型）。踩坑:`helm template` 因 secret.yaml fail-close 缺 masterKey→须 `--set existingSecret=x`。
- **M5.1c 观测收尾 `001cc4f`**（用户 greenlit；一次性放宽=仅限纯 additive 可观测）：接线 3 处 stubbed TODO，授权决策/fail-close/apply/循环/签名逐字不变。① authz.go(核心) Enforce 后仅内部错误 `obs.From(ctx).Warn`（合法拒绝!allow 不记），返回逐字不变——测试关 DB 触发 Enforce 内部错误（scopeTenant ListApplications，Enforce 前不碰 DB；`adminauthz.Enforcer.Enforce` 每次 `ReadPolicyVersion` 故关 DB 必错）断言仍 PermissionDenied+日志有齿。② relay.go drain 错误 Warn+run.go onElected 注入 `obs.With(lctx,...)`；`drainOnce` Publish 失败是 break 下轮重试→测试关 DB 触发。③ sidecar `syncclient.Config.OnSnapshotApplied` 回调，resync 成功 ApplySnapshot 后触发单一 hook，run.go 接 `m.SnapshotApplied`。obs 无 import 环。已 push origin。

## M6 商业化+合规+GA（进行中，最后里程碑）

范围=计费/配额/用量计量、企业 SSO(SAML/OIDC)、合规(审计留存/数据驻留/GDPR/SLA·SLO)、渗透、API 版本化、GA 文档。**计费/SSO/合规涉产品/供应商选型（路线图 §6 留 brainstorm=用户战略决策），不擅自猜**；自主先做决策无关、纯工程、GA 前置片。

- **M6.5 API 版本化+向后兼容 `81a3d7f`**（纯 Makefile+docs）：`make proto-breaking`（`buf breaking --against` 默认 main）+`docs/api-versioning.md`（v1 wire+JSON+源码兼容契约/演进表/弃用/v2/GA tag 冻结基线）；变异字段改名对 main FAIL 证有齿；抓修 Makefile `#` 注释截断 bug→字面量须 `\#`。
- **M6.1a 应用配额 `73efd45`**（触碰管理写路径零触碰授权核心）：迁移 000021 `plan` 表数据驱动限额(free3/pro50)+`tenant.plan_id`；`CreateApplication` `FOR UPDATE OF t` 锁租户行+计数达 `max_applications` fail-close `ResourceExhausted`(REST→429)；8 并发 free(3)→行锁串行恰 3 成功。踩坑 `uq_tenant_domain` 需 distinct domain、`mgmt_test` 外部包 fixture 返 db 句柄。
- **M6.1b 计量 RPC `GetTenantUsage` `a271e82`**：加 additive RPC 全套面(proto+`buf generate`+store+handler+ruleTable+restgw+apidoc)返 `{plan,used,limit}`；gRPC+REST parity、scopeTenant 授权、只读 `TenantUsageOf` 无锁；过 proto-breaking 验兼容门。
- **M6.1c Console 用量页 `a192f63`**（消费 GetTenantUsage 纯读第四面，机器 diff 仅 7 文件全 console/）：独立页 `GET /tenants/{id}/usage`（与 `/members` 同构，unknown→404 跨租户→403 三面一致）；严格 CSP→值驱动进度条唯一安全解=原生 `<meter min max value>`（值经属性非内联样式，`templates_lint` 挡内联 style/script）；`bizterm.planLabel`；at-limit `.alert-error`（渲染+告警双向有齿）；真浏览器 a11y 快照核实 landmark/单 h1/meter role 干净。
- **M6.1d 成员配额 `e7b63bb`**（第二维度端到端 enforce+measure+UI；机器 diff 空、proto additive）：**关键=Order-B 门（insert-then-gate）**——`InviteMember` 先 `InsertMembership` 拿 `inserted`,`!inserted` 短路 `AlreadyExists` 再对新成员查 `count>=MaxMembers`→`ResourceExhausted`；避 Order-A 语义 bug（满员重复邀请已有成员误判 ResourceExhausted）；`TenantPlanLimits` `FOR UPDATE OF t` 早锁串行（8 并发 owner 占 1→恰 2 成 6 exhausted）。踩坑:成员门破分页测试→测试内 `UPDATE plan SET max_members=1000 WHERE name='free'` 解耦。抓修 latent bug:Console `errors.go httpStatusForCode` 无 `ResourceExhausted`→落 default 500 吞配额文案→加 `ResourceExhausted→429`。用量页重构为资源行列表（`usageRow`+`makeUsageRow`，`range .Rows`）。
- **M6.1e dashboard 内联用量提示 `596bb60`**（fail-soft，机器 diff 仅 3 文件全 console/）：`h.tenantUsageRows` 复用 M6.1d `usageRow`（放 routes_apps.go 零新增 import）——任何 err→nil；dashboard `if tid!=0` 才取（超管全量 tid=0 跳过；降级 PermissionDenied 分支不动）；模板紧凑行「配额：应用1/3 · 成员0/3 · 详情」，至上限「已达上限」摆创建点。
- **M6.1f 成员页内联成员配额提示 `b5732b6`**（dashboard 对称 fast-follow；本会话 M6.1c→M6.1f 19 提交 `a271e82..b5732b6` 已 push origin）：DRY 重构把 `tenantUsageRows` 拆为更低层 `tenantUsage(ctx,principal,tid) *GetTenantUsageResponse`（共享 fail-soft）；members 组成员行 `data["MemberUsage"]=makeUsageRow("成员",u.Members)`，只示成员维（app 配额在此页离语境）。**M6.1 计量+配额基座闭环**（M6.1a 强制+b RPC+c UI+d 成员维+e/f 内联提示）。成员配额 UI 未做真浏览器走查（复用 M6.1c 已核实 meter 结构，纯静态无 JS）。
- **M6-GA-docs 文档导航 `37c21be`**（纯文档，已 push origin）：`docs/README.md`（评估/部署/运维/API 分组地图零复述）+顶层 README 文档导航节+helm README 补 HPA/SM values 行。21 链接死链核验 0。
- **M6-security GA 安全评审 `eecb189`**（纯文档，已 push origin）：全信任边界 GA 前置源码级审查，逐条附 file:line。**零可利用漏洞**。已验证强项:AES-256-GCM 主密钥不入库/深拷贝、HMAC-SHA256 `hmac.Equal`+±5min 窗+method 绑定、枚举 Oracle 闭合、空密钥/多租户域 fail-close、CheckStatusWrite 装配契约、停用 operator 即锁死、会话 CSPRNG+Secure/HttpOnly/SameSite=Strict+CSRF 常量时间、错误脱敏、无 SQL 注入/XSS/开放重定向。**发现 4 条**：**F-1[最要紧]**——应用「停用」经 `secret.Resolver.ResolveSecret`(`secret/resolver.go:36`)不校验 application.status，与 operator 侧不对称→停用只冻结管理面写(CheckStatusWrite)，被停用 app sidecar 仍能 HMAC 拉快照；真正运行时吊销=`RotateApplicationSecret`(重加密旧 HMAC 即失效,`admin_ops.go:419`)→须手册讲清「轮换即吊销、停用非吊销」；policysync 也校 status 的对称加固须用户 greenlight。F-2 重放窗口(5min 无 nonce)、F-3 登录无速率限制(secret 高熵非真漏)、F-4 会话滑动 TTL 无绝对上限。

- **M6-sso-1 每租户 OIDC IdP 配置地基（企业 SSO 第一片；spec+plan 本会话前已在 main〔`f17969b`spec`51ad002`plan〕，本会话以 subagent-driven-development 执行 5 任务；本地 main 领先 origin 8 提交待 push）**：纯配置 CRUD+加密，**不触认证/登录路径**（供应商无关地基，登录流留下一片）。**授权求值核心零触碰**（authz.go 仅 +2 ruleTable 条目 `{"sso","update",false,scopeTenant}`+`{"sso","read",false,scopeTenant}`，非求值逻辑；超管 `*` 域通配覆盖新 resource 无需 seed；机器 diff casbin/adminauthz/kernel/dataperm 空）。① 迁移 000024：`tenant_idp`（一租户一 IdP `uq_tenant_idp_tenant`，`client_secret_enc BYTEA`）+`tenant_idp_domain`（email 域**全局唯一** `uq_tenant_idp_domain`=一域→一租户不歧义，为下一片按域路由登录铺路）。② store：`TenantIdp` 读视图（**绝不含 secret 字段**=结构性脱敏）+`UpsertTenantIdpTx`（事务内 upsert+DELETE 后逐条 INSERT 替换域集合，`ToLower+TrimSpace` 小写化，重配本租户自己的域不自冲突）+`TenantIdpOf`（不 SELECT `client_secret_enc`；无配置→`Configured=false` 非 ErrNotFound〔有意:SSO 未配置是租户正常态〕）。③ proto additive ConfigureTenantIdp+GetTenantIdp（`GetTenantIdpResponse` 无 secret 字段；过 proto-breaking，另跑针对 51ad002 基线的有意义对比证纯 additive）。④ handler：`ConfigureTenantIdp`（**scopeTenant 租户 owner 自助**；`crypto.Encrypt(masterKey)` 加密 secret；域被他租户占用 `isUniqueViolation`→AlreadyExists；审计 `configure_idp` diff 白名单 issuer/client_id/domains/enabled **绝不含 secret**〔INV-1〕）+`GetTenantIdp`（脱敏读）；REST `PUT`/`GET /v1/tenants/{id}/idp` 照 ChangeTenantPlan 同款。**INV-1 全链路核实无泄露面**:DB 存密文·store 不查 secret·proto 无 secret 字段·审计白名单·错误不含明文（secret 是 bytea 绑定参数不进 SQL 文本）。变异两处证有齿:A 撤 isUniqueViolation→DomainConflict 测试红(AlreadyExists→Internal)、B 撤加密→secret 明文入库 EncryptsAndGetOmitsSecret 测试红。全仓 `go test ./...` 45 包全绿。commits `e9005ae`(T1 迁移)`a84a7fc`(T2 store)`7b5d753`(T3 proto)`f4e0054`(T4 handler+ruleTable+REST)`f085c4e`(复审修复)`54ec387`(研磨)。**下一片=OIDC 登录流（按 email 域路由到租户 IdP），须独立 spec；tenant_idp_domain 全局唯一已铺地基。**
  - **复审抓修真 bug（二段审查价值实证）**:opus 整体终审+T4 质量审查发现 `ConfigureTenantIdp` 对**未知 tenant_id** 未 catch 外键违例(23503)→裸 pq 错误(含表名/约束名)以 `Internal` 透传(既泄露又码不合语义)。可达:scopeTenant 但持 `*` 域通配 grant 的超管可对任意/拼错 tenant_id 触达 handler。修复(`f085c4e`):复用既有 `isForeignKeyViolation`(admin_ops.go:98) 映射 `NotFound "unknown tenant"`（与兄弟 handler `ChangeTenantPlan`〔billing.go:31,同 `/v1/tenants/{id}/…` REST〕一致；协同 M6-errsem 裸 Internal 排除慣行）。变异证有齿:注释掉 FK 分支→UnknownTenant 测试红(NotFound→Internal)。
  - **研磨(`54ec387`,用户 greenlit #2+#3)**:① 空/纯空白域校验→InvalidArgument（防空串域占全局唯一 `""` 槽污染下一片域路由）;② `GetTenantIdp` 跨租户隔离测试（读路径也 PermissionDenied）+ owner 正路径测试（`AuthorizeRule` 经真 casbin;**未知 method 默认 PermissionDenied=fail-closed 故正路径 NoError 非伪阳性,与跨租户拒绝测试互补=method 拼错会翻正路径为红**）。**未做域去重（#1 计划明确延期;同请求内归一后重复域→AlreadyExists 消息「他租户占用」对自重复略不精确=已知延后项,行为安全事务回滚）。**
  - **subagent-driven 执行涌现**:(1) 环境 git 日期固定使 commit SHA **决定论可复现**——子代理照计划精确代码 TDD 重跑落与前会话逐字符相同的 hash（tree+parent+msg+author+固定 date 全同）。(2) `git commit --amend` 被**权限系统拒**→复审修复/研磨均改独立提交（更可审计;协调者指示≠用户本人批准,子代理正确未绕过权限拒绝）。(3) 每任务 实现者→规格合规审查→代码质量审查 二段审查;审查者自带变异/伪阳性核验（如查 `AuthorizeRule` 未知 method 默认行为排除正路径伪阳性）。

### 未提交工作树（早先 M6-errsem+M6-SLO 已 push 至 3779760；当前未 push=M6-sso-1 全部）

- **M6-errsem 写错误语义细化**（闭合全仓唯一剩余代码 TODO `server.go:43`；触碰 mgmt 写 API+policy 写引擎，**零触碰授权核心**〔机器 diff casbin/adminauthz/kernel/dataperm/authz.go 空〕）：先前 `PolicyManager` 全部写错误被 mgmt 一律压成 `codes.Internal`→再被 `SanitizeErrorUnaryInterceptor` 脱敏成「internal error」，丢失可回传语义码。①`policy/errors.go` 新增 sentinel `ErrInvalidInput/ErrConflict/ErrNotFound/ErrPrecondition`+`classify(err)`（`%w` 二次包裹保留原链供日志，errors.Is 可判）+`isPGCode`——映射:pq 23505→Conflict、pq 23503+`projection.ErrCycle`→Precondition、`store.ErrNotFound`/`sql.ErrNoRows`→NotFound；②`manager.go` runVersionedWrite/Data 的 preCheck/mutate/apply 出错套 classify、ApplyTemplate `':'` 校验用 ErrInvalidInput 包；③`mgmt` 新增 `mapWriteErr(what,err)`（errors.Is switch→AlreadyExists/FailedPrecondition/NotFound/InvalidArgument，default Internal）替 8 处裸 Internal（server.go writeResp/CreateRole/UpsertPermission/UpsertDataPolicy/batchResp+business_role/templates/tenant_templates）。**安全铁律:分类码绕过脱敏拦截器直达客户端→回干净固定文案,绝不插值 raw pq 详情;仅 Internal 路径插值。** **涌现:`console/errors.go`+`restgw/errors.go` 早已把 AlreadyExists/FailedPrecondition→409、NotFound→404、InvalidArgument→400 映射好,只是此前写路径一律 Internal 从没送达→本改动端到端送达=严格改进,下游零改。** TDD:classify+mapWriteErr 单元测试+端到端集成(真 AdminServer 重复 code→AlreadyExists、Grant 不存在 role/perm→FailedPrecondition)。两变异:删 classify 唯一 case→红；去 mapWriteErr Conflict 分支→码 6 变 13 红。既有 `manager_rbac_test` `ErrorIs(err,projection.ErrCycle)` 仍绿双证保链。build+vet 0、policy/mgmt/console/restgw 四套件全绿。5 文件改+4 新。保留未做:store data_policy `id not found` 裸 fmt.Errorf 未转 sentinel(narrow)；ImportAppPolicy 走自有 ErrImport* 不并入。
- **M6-SLO 服务水平目标+Prometheus 告警**（M6 合规范围 SLA/SLO；纯 deploy/helm+docs 零 Go；operationalize M5.1 指标+M5.3-k8s-ext ServiceMonitor+M5.5 基线）：**强协同 M6-errsem**——可用性 SLO 只计服务端故障码 `code=~"Internal|Unknown|Unavailable|DataLoss"`,排除客户端错误,而 `code` 区分正是 error-semantics 刚解锁。①`values.yaml` 加 `prometheusRule` 块(enabled 默认 false、labels、可调阈值 grpcFaultRatioWarning0.01/Critical0.05/checkLatencyP99Seconds0.05/cacheHitRatioMin0.8，据 M5.5 基线推荐默认);②`templates/prometheusrule.yaml`(monitoring.coreos.com/v1 PrometheusRule，门控默认关，同 ServiceMonitor 范式)——4 记录规则(grpc/http 服务端故障率、authz Check p99、cache 命中率)+8 告警(可用性 warning+critical、REST 5xx、Check 延迟、命中率低、sidecar 断连、relay 无 leader/脑裂)，`$value humanize*` 用 ``{{` `}}`` 转义避 helm 吃掉;③`docs/runbooks/service-level-objectives.md`(SLO 定义+告警→处置 runbook+阈值调优+错误预算+度量前提〔CP vs sidecar 指标须同 Prometheus 抓〕)+docs/README 运维分组+helm README values 行。**指标名全部回源核实存在于 `internal/obs/metrics.go`**:`sydom_grpc_requests_total{code}`(code=`status.Code(err).String()` CamelCase)、`sydom_http_requests_total{code}`(int 5xx)、`sydom_authz_check_duration_seconds`(histogram)、`sydom_cache_hits/misses_total`、`sydom_sidecar_connected`/`sydom_relay_leader`(gauge)。**涌现:relay leader SLO 直接消费 M5.4a 选主 gauge(sum==1;0=outbox 不 drain,>1=脑裂)。** 工具缺 promtool/yamllint→验证=helm lint 0+helm template 两态(默认 0 条/enabled 渲染)+python `yaml.safe_load_all`(6 文档)+指标名交叉核验(7 名全 OK)。变异:改 `_seconds`→`_secondz`→交叉核验报 MISSING。5 链接 0 死链。**M6 自主工程片 error-semantics+SLO 两片续做完；再往后 SLO 告警需真集群 Prometheus 验证(本地无)，大件仍 计费/SSO/合规框架需用户产品决策。**

### M6-sso-2 OIDC 登录流（企业 SSO 第二片；已 push origin，`origin/main==本地 main==3c78b1f`）

- **范围**：租户 operator 经其租户 OIDC IdP 登录 Console——发起→授权码流(PKCE S256)→ID Token 验签→**事前登录严格映射**（无 JIT）→复用既有 `console/auth.go handleLoginPost` 的 `sessions.Create(principal)`+cookie 汇聚点。SSO 只是多一条前门认证路径，**会话/RBAC/授权求值核心完全不变**。SAML/JIT/端用户登录/单租户多 IdP 留后续片。
- **流程**：brainstorm→spec(`42311e5`)→writing-plans(`d875032`，5 任务)→subagent-driven-development（中途撞会话上限 API 错→转纯内联 TDD）。
- **T1 迁移 000025 `5a43e7a`**：`admin_operator ADD COLUMN email VARCHAR(320) UNIQUE`（nullable；Postgres UNIQUE 允多 NULL 故存量 operator 不受影响；小写化由应用层保证）。迁移测试断言 UNIQUE 拒重复+多 NULL 共存+down 删列（testcontainers postgres:17）。
- **T2 `internal/oidc` 纯 stdlib RP `f48978e`**（无新依赖，契合既有 hand-rolled crypto；Go module proxy 冷启动偶发 EOF 故 stdlib 手写零 fetch 风险）：`oidc.go`（`Discover` GET well-known+**校 issuer 字段==请求 issuer 防 mix-up**；`AuthCodeURL` scope=openid email+code_challenge_method=S256；`Exchange` POST token client_secret_basic 取 id_token；`PKCEChallenge`=base64url(sha256(verifier)））+`jwks.go`（`ParseJWKS` RSA n/e→big.Int、EC crv=P-256 x/y；`FetchJWKS`）+`verify.go`（`VerifyIDToken` 逐条固定：拆 header.payload.sig→**alg 白名单仅 RS256/ES256 拒 none/HS***→按 kid 选公钥+**kty 与 alg 族绑定**→验签 RS256=VerifyPKCS1v15/ES256=ecdsa.Verify〔r,s 各 32B〕→iss/aud∋clientID/exp〔leeway≤60s〕/iat/nonce 全校验；`ErrUnknownKID` 哨兵供刷新 JWKS 重试；`audience` 自定义 Unmarshal 兼容 string 与 []string）。测试 RSA 私钥签 token+构造 JWKS，正路径+**9 条负路径逐条**（改签名/换签名密钥/none/HS256/未知 kid/错 aud/错 iss/错 nonce/已过期）+httptest mock discovery/exchange/jwks。**变异有齿关键洞见**：撤 alg 白名单不只是撤一道守卫——verify switch 只有 RS256/ES256 分支无 default，故未知 alg（none/HS256）**直接跳过验签**被接受；变异证 none+HS256 两测试同时翻红（whitelist 是唯一守卫）。
- **T3 operator email 开通 `86f8c47`**：proto additive `CreateOperatorRequest.email=2`（可选空=NULL）+新 rpc `SetOperatorEmail(SetOperatorEmailRequest{principal,email})→WriteResponse`（过 `make proto-breaking`）；handler `CreateOperator` 落 email 用**同事务 follow-up UPDATE**（**不改冻结的 `adminauthz.InsertOperator`**=保零触碰）、`SetOperatorEmail`（email 不参与 casbin 策略故不 bump 版本；冲突 isUniqueViolation→AlreadyExists、无 operator RowsAffected==0→NotFound、空 email→NULL 清除、审计 `set_email` 只记 `email_set` bool 不记 secret）；ruleTable +1 `{"admin","update",false,scopeSystem}`（与 CreateOperator 一致=平台超管）；REST `PUT /v1/operators/{principal}/email`。
- **T4 Console 登录编排 `3c78b1f`**：① store `idp_login.go`（`IdPLoginByDomain` JOIN tenant_idp+tenant_idp_domain〔**双表有 tenant_id→列须 `ti.` 限定消歧**〕、`IdPLoginByTenant`、`OperatorEmailMatch`=`admin_operator(email+status=1)` ∩ `tenant_membership(tenantID)` 严格映射 fail-close）；② **新 leaf 包 `internal/controlplane/ssologin`**（`Resolver{db,masterKey}` 实现 console 私有窄接口 `idpLoginResolver`/`operatorMatcher`，`crypto.Decrypt` 解 client_secret；imports store+crypto **非 console/adminauthz**=避触碰冻结 adminauthz 又守 INV-1〔secret 解密留控制面〕，且避 console→mgmt 既有依赖成环）；③ `console/oidcstate.go`（Redis 一时态键 `console:oidcstate:<state>` TTL 10min，`TakeOIDCState`=原子 **GETDEL 一次性**防重放，存 `{nonce,verifier,tenantID,returnTo}` 不含 secret/issuer）；④ `console/oidc.go`（`handleSSOStart` POST /login/sso：email 域路由→discovery→生成 state/nonce/verifier〔复用 randToken CSPRNG〕→存一时态→302；`handleOIDCCallback` GET /auth/oidc/callback：state GETDEL→**按一时态 tenantID 重取 IdP**〔不信回调参数〕→Exchange→FetchJWKS+VerifyIDToken〔kid 未知刷新重试一次〕→`claims.EmailVerified` 须 true→严格映射→`sessions.Create`→302 returnTo；`isSafeReturnTo` 仅本站相对路径防开放重定向；`ssoFail` 统一通用 401 无枚举 oracle）；⑤ `login.html` 加 email 先行 SSO 表单+保留 principal/secret 回退；⑥ 装配 `NewHandler` 加 `sso SSODeps{Resolver,Matcher,HTTPClient,ConsoleBaseURL}`（零值=未装配 SSO 路由 fail-close）、`run.go` 传 `ssologin.NewResolver(db,masterKey)`+10s 超时 http.Client、`config.go` 加 `ConsoleBaseURL`（`SYDOM_CONSOLE_BASE_URL`，**不从请求 Host 派生**可伪造，空→SSO fail-close）。**mock IdP 端到端测试**（httptest serve discovery+JWKS+token 签真 RS256 token）6 条：正路径〔active 成员→会话 cookie〕、state 重放〔第二次→401〕、跨租户冒充〔email 属 A 但 operator 仅 B 成员→401〕、email_verified=false→401、IdP disabled→发起即 401、缺 consoleBaseURL→fail-close 401。
- **T5 全局验证**：全仓 `go test ./...` **43 包 ok 0 FAIL**（+10 no-test）；`make proto-breaking` 过；**零触碰机器 diff**：`casbin/`·`internal/controlplane/adminauthz/`·`internal/sidecar/kernel/`·`internal/sidecar/dataperm/` 对 `origin/main`（54ec387）**空 diff**，`mgmt/authz.go` 仅 +1 行 ruleTable（`SetOperatorEmail`，非求值逻辑）；两变异复核有齿（T2 alg 白名单、T4 撤 `m.tenant_id=$2` 跨租户断言红）。生成码 `admin.pb.go` 大 diff=同 protoc-gen-go v1.33.0 rawDesc 重编码churn，新增符号仅 `GetEmail`/`SetOperatorEmailRequest`+其 gRPC plumbing（无版本漂移）。
- **INV-1 全链路无 secret 泄露面**：一时态不含 secret（回调按 tenantID 重取）·ssologin 解密仅进程内做 token 交换·绝不入 Redis/日志/审计/API 响应。
- **执行涌现**：subagent-driven 分派实现子代理时命中 **Anthropic 会话上限（`You've hit your session limit · resets 6:40am`）**——遂**转纯内联 TDD** 逐任务续做，质量关卡（test-first、两变异、零触碰机器 diff、go vet）未降；每任务独立 commit 本地 FF。
- **后续片（各需独立 spec）**：JIT 自动开通；SAML；端用户/下游应用登录（Sydom 作 IdP broker）；单租户多 IdP；operator email 的 Console 管理页；RP-initiated/back-channel 单点登出；`sub` 锚（比 email 更稳）。域去重仍延期（承 M6-sso-1）。

### M6-sso-3 OIDC JIT 自动开通（企业 SSO 第三片；已 push origin，`origin/main==本地 main==9b8696b`）

- **定位**：消除 M6-sso-2 事前严格映射的摩擦（管理员须逐一预建 operator+设 email，企业量级不可扩展）。租户 owner 开 `jit_enabled` 后，其 IdP 域下 email_verified 的**全新**用户首登自动开通为**零权限成员**并复用会话。
- **流程**：用户选「下一 SSO 片」→AskUserQuestion 选定 JIT（vs SAML/多IdP/sub锚）→brainstorming 三决策→spec(`740bf55`)→writing-plans(`cb56bd2`)→纯内联 TDD。
- **brainstorm 三决策（均用户 AskUserQuestion 选）**：① 门控=每租户 `jit_enabled` 开关默认关（vs 域信任常开/allowlist）；② 默认权限=最小（operator+TierMember+零 casbin 绑定，vs 只读 viewer 角色/租户管理员）；③ 既有 email=仅建全新（vs 自动把既有 operator 加为成员——后者是账户接管面）。
- **T1 迁移 000026 `35382b5`**：`tenant_idp ADD COLUMN jit_enabled BOOLEAN NOT NULL DEFAULT false`（默认 false=存量 IdP 保持严格，向后兼容）。
- **T2 供应原语 `9c003da`**：① `store/idp_login.go` 登录读路径 `idpLoginCols` 加 `ti.jit_enabled`、`IdPLoginRow.JITEnabled`；新 `InsertJITOperatorTx`（`INSERT admin_operator(principal, secret_enc, email, status) VALUES ($email,$enc,$email,1)`——**principal=email**）。② `ssologin.IdPLogin.JITEnabled` + `ProvisionOperatorForLogin(ctx, tenantID, email)(principal, ok, err)`：单事务 `SELECT EXISTS(email)`→既有则 `ok=false`（不 JIT）→随机 32B secret `crypto.Encrypt`→`store.InsertJITOperatorTx`（UNIQUE 竞态/超长 email→err fail-close）→`adminauthz.InsertMembership(TierMember)`（**首次签发 tier 3**）→`adminauthz.InsertAdminAudit("sso_jit","jit_provision","operator",email,{via:sso_jit})`〔不含 secret〕→**不 BumpPolicyVersion**（零 casbin 绑定=零策略变更，enforcer 无需重载；account-layer I-1 平凡满足）。**ssologin 复用 adminauthz 公有函数不改源**（imports adminauthz，无环：adminauthz 不 import ssologin/console/store）。
- **T3 回调 JIT 分支 `266f5e3`**：`console/auth.go operatorMatcher` 接口 additive 加 `ProvisionOperatorForLogin`；`handleOIDCCallback` 严格映射未命中后 `if idp.JITEnabled { principal,ok,err = h.operatorMatch.ProvisionOperatorForLogin(...) }`，`if err||!ok → ssoFail`（JIT 关/既有 email/竞态→通用 401 无枚举 oracle）。mock IdP 4 场景：JIT 开+全新→开通零权限成员〔断言 tier=3+`admin_subject_role` 无该 principal 行〕、JIT 关+全新→401〔回归守卫〕、JIT 开+既有非成员→401〔跨租户防护〕、JIT 开+既有成员→严格映射胜不重复开通。
- **T4 开关配置面 `9b8696b`**：additive proto `ConfigureTenantIdpRequest.jit_enabled=7`+`GetTenantIdpResponse.jit_enabled=6`；`store.TenantIdp.JITEnabled`+`UpsertTenantIdpTx` 加 `jitEnabled` 参数〔破坏 3 处调用方:mgmt/sso.go 生产 + tenant_idp_test.go 两处测试直调，均补末位实参〕+`TenantIdpOf` SELECT；handler `ConfigureTenantIdp` 透传 `r.JitEnabled`+审计加 jit_enabled、`GetTenantIdp` 回显。过 proto-breaking。
- **T5 验证**：全仓 `go test ./...` **43 包 ok 0 FAIL**；`make proto-breaking` 过；**零触碰机器 diff `casbin/`·`adminauthz/`·`sidecar/kernel/`·`sidecar/dataperm/`·`mgmt/authz.go` 对 `origin/main`〔3c78b1f〕全空**（本片授权侧+authz.go 一字不改，比 M6-sso-2〔+1 ruleTable〕更干净——JIT 不新增 RPC，开关搭 ConfigureTenantIdp，开通在回调内）；go vet 干净。
- **涌现/教训**：
  - **变异实测纠错（诚实记录）**：writing-plans 原设「撤 `ProvisionOperatorForLogin` 的 `if exists` 守卫→`TestOIDCLogin_JITRejectsExistingNonMember` 红」——**实测不翻红**：既有 email 的 JIT INSERT 撞 `admin_operator.email` UNIQUE 约束→err→fail-close，UNIQUE 是冗余第二道防线。故 exists-guard 是清路径优化+清晰语义（返 ok=false 而非 UNIQUE-violation err），真正强制不变量的是 **email UNIQUE + 严格映射租户成员条件**（后者 M6-sso-2 已证有齿）。改用 **TierMember→TierOwner 变异**（tier=3 断言翻红）证「最小权限档位」+ **JIT-gate `idp.JITEnabled`→`true` 变异**（JIT-关测试翻红）证「门控」。计划文档已内联记此纠错。
  - **principal=email 但 `admin_operator.principal VARCHAR(128)`**：超长 email(>128，罕见)INSERT 失败→fail-close（安全，已在 store 注释+计划标注；未加宽 principal=避触碰 casbin subject 列）。
  - 继续纯内联 TDD（subagent 会话上限自 M6-sso-2 起持续）。
- **后续片（各需独立 spec）**：JIT 默认非零权限（只读 viewer 角色=UX 决策）；JIT 更新既有账户/离职反向 deprovision；SCIM/群组→角色映射；承 M6-sso-2 的 SAML/单租户多 IdP/sub 锚/单点登出/operator email 与成员的 Console 管理页。

### M6-sso-4 Console IdP 配置页（企业 SSO 第四片 UX 收尾；已 push origin，`origin/main==本地 main==8320fb5`）

- **定位**：企业 SSO 三片（配置地基/登录流/JIT）后端闭环，但 IdP 配置**仅 REST/RPC** 可用，租户 owner 无法经 Console UI 自助配——与「双层 UX」定位不符。本片补 Console 页。用户 AskUserQuestion 选此方向（vs SSO 增强/换领域）。
- **brainstorm 核心取舍**：`GetTenantIdp` 绝不回 client_secret（INV-1）→编辑表单无法回填 secret。决策=**留空=保持不变**（vs 每次保存重输 secret——烂 UX）。实现 **Variant A**（复用 `UpsertTenantIdpTx` 不改 SQL，加只读密文小函数把旧密文原样回写；vs Variant B UPDATE-only 不碰 secret 列——A 少活动部件，用户认可）。
- **T1 后端 `948045b`**：① `store.TenantIdpSecretEnc(ctx, ex, tenantID)([]byte,bool,error)` 读原始 `client_secret_enc` 密文（**唯一用途=编辑保留原样回写，从不解密〔不碰 masterKey〕、绝不出控制面**，INV-1）。② `ConfigureTenantIdp` 放宽：校验去掉 `ClientSecret==""` 必填；encrypt 挪进 tx 后分支——空 secret→`TenantIdpSecretEnc(tx)` 取旧密文复用（无配置→`InvalidArgument`「首次配置须提供 secret」），非空→`Encrypt`；`UpsertTenantIdpTx` 签名/SQL 不变；审计 diff 加 `secret_rotated: r.ClientSecret != ""`。**REST/RPC 同步获得空-secret 语义**（同 handler，additive 放宽，旧调用方总带 secret 故行为不变）。测试：首次空 secret→InvalidArgument、编辑空 secret 切 jit→**DB 密文字节不变**、编辑带新 secret→密文变（轮换）。
- **T2 Console 页 `8320fb5`**：① `routes_idp.go`——`idpConfig` GET（照 `routes_usage.go`：requireSession→pathUint64→`AuthorizeRule(GetTenantIdp)`→`h.srv.GetTenantIdp`→renderPage，**secret 永不预填**，`sess.CSRF` 传模板）；`idpSave` POST（`doWrite(ConfigureTenantIdp)` 管线，decode 从 form 建 req，`enabled/jit_enabled` checkbox=`PostFormValue!=""`，domains textarea `splitLines` 按行 trim 去空，client_secret 不 trim 空=保持）。② `idp.html`（照 members.html：单 h1、label 关联、csrf 隐藏、无内联 style/script 过 `TestTemplates_NoInlineStyle`；client_secret 占位「留空=保持不变」/未配置 required；jit 复选框+内联安全提示「全新用户→零权限成员」）。③ `flash.go` 加 `ConfigureTenantIdp: "IdP 配置已保存"`；`handler.go` `registerIdP`。④ **`tenants.html` 加「企业 SSO」入口链接**（否则页面无处可达=孤儿页；计划未列，实现时补）。测试：GET 未配置/已配置渲染（断言无 s3cr3t）、POST 新建（域空行丢弃 acme/foo=2 行）、POST 编辑空 secret 切 jit→密文保留；PageSweep/lint 全过。
- **T3 验证**：全仓 `go test ./...` **43 包 ok 0 FAIL**；`make proto-breaking` 过（本片无 proto 改）；**零触碰机器 diff `casbin/`·`adminauthz/`·`sidecar/kernel/`·`sidecar/dataperm/`·`mgmt/authz.go` 对 `origin/main`〔9b8696b〕全空**（无新 RPC、无 ruleTable、无授权逻辑改）；go vet 干净。**变异有齿**：把空-secret 分支 `enc=existing` 改成 `Encrypt(r.ClientSecret)`〔即 `Encrypt("")`〕→保留-密文断言红。
- **涌现/教训**：(1) 孤儿页——新 Console 页须在 `tenants.html` 加入口链接才可达（计划漏列，实现走查补）。(2) INV-1 更强的实现本可取 Variant B（永不碰 secret 列），本片取 A（读密文原样回写，从不解密）少一个函数；两者安全等价。(3) 继续纯内联 TDD（subagent 会话上限持续）。
- **后续片（各需独立 spec）**：删除 IdP（无 DeleteTenantIdp RPC）；IdP 连通性测试按钮（discovery 探活）；operator/成员 email 的 Console 管理页；承前的 SAML/多 IdP/sub 锚/单点登出/JIT 默认角色。

### M6-sso-5 IdP 删除 + 连通性测试（企业 SSO 第五片 管理收尾；已 push origin，`origin/main==本地 main==198c4d8`）

- **定位**：sso-4 Console IdP 页有创建/编辑，缺删除/探活。补齐 CRUD+运维。用户 AskUserQuestion 选此方向。
- **brainstorm 两决策**：① 删除门控=二次确认+锁死警示，不阻断（照 DeleteDataPolicy 范式；管理员决定）；② 连通性测试目标=**仅探已保存 issuer**（=登录流同一出站 URL，不新增 SSRF 面）vs 探表单未保存 URL（会让已认证 operator 探内网=新 SSRF 面）。
- **T1 删除后端 `5d5d3df`**：proto additive `DeleteTenantIdp(DeleteTenantIdpRequest)→WriteResponse`；`store.DeleteTenantIdpTx`（`DELETE tenant_idp_domain`〔**域表引用 tenant 非 tenant_idp 不级联，须显式先删**〕+`DELETE tenant_idp`，返回 RowsAffected>0）；handler（scopeTenant，无配置→NotFound，审计 `delete_idp` diff=nil，**不级联删 operator/membership**=已开通账户保留仅失 SSO 登录）；ruleTable +1 `{"sso","delete",false,scopeTenant}`（超管`*`+租户 owner `(t:*,*,*)` 覆盖，无需 seed）；REST `DELETE /v1/tenants/{id}/idp`。
- **T2 Console `198c4d8`**：① `idpDelete`（`requireConfirm(DeleteTenantIdp)` 二次确认→`doWrite`；confirm.go 加锁死警示文案）。② `idpTest`（**Console-only 零新增 RPC/ruleTable**：session→checkCSRF→`AuthorizeRule(GetTenantIdp)`→`GetTenantIdp` 取已保存 issuer→`!Configured` 先拦「请先配置」→`oidc.Discover`+`FetchJWKS`→结果 `SetFlash`→302；layout 渲染 flash toast）。③ idp.html `{{if .Configured}}` 内加测试/删除独立表单（POST+csrf，过 lint）。
- **T3 验证**：全仓 43 包 ok 0 FAIL；proto-breaking 过；**零触碰机器 diff casbin/adminauthz/kernel/dataperm 对 `origin/main`〔8320fb5〕空，`authz.go` 仅 +1 ruleTable（delete）**；go vet 干净。**两变异有齿**：① 撤 `DeleteTenantIdpTx` 域删除→`TestDeleteTenantIdp` 域清空断言红；② 撤 `idpTest` 的 `!Configured` 先拦→`TestIdPTest_UnconfiguredHint`「请先配置」断言红（探空 issuer 得「连通测试失败」）。
- **涌现/教训**：连通性测试测试需 `h.oidcHTTP`（`newConsole` 为 nil，`newConsoleSSO` 已设）→用 `newConsoleSSO` + `loginClient` + 新 `extractCSRF`（正则从渲染页取 csrf_token）避免改 `newConsoleSSO` 签名/触碰既有 6 个 oidc 测试。SSRF 缓解=只探已保存 issuer（登录流同 URL 无新面）+POST+CSRF+超时+通用失败文案。
- **企业 SSO 五片全闭环**：地基(sso-1)→登录流(sso-2)→JIT(sso-3)→Console 配置页(sso-4)→删除+探活(sso-5)。**后续片（各需独立 spec）**：SAML、单租户多 IdP、sub 锚、单点登出、JIT 默认只读角色、operator/成员 email 管理页。

## 开放项 / 技术债

- **dangling 分支 `worktree-feat+sp3-console`**：已核实可安全删（`git cherry -v main` 标 `-`=等价提交已入 main；唯一独有提交 d989045 与 main 字节相同）——harness 拦 `git branch -D`，须用户亲跑 `! git branch -D worktree-feat+sp3-console`。
- **M6 前收口审计**：5 处 Go TODO 曾是有意延后（代码内注明），其中 3 处已由 M5.1c 接线、`server.go:43` 已由 M6-errsem 闭合；剩 `mgmt/server.go` PolicyManager sentinel 细化仍开放（见 M6-errsem「保留未做」）。
- **次要技术债**：M5.3-k8s Ingress 延后（console 未入 chart+gRPC controller 决策）；M5.5 BatchEnforce 缓存优化✓（子代理落地 push origin `d18f134`，~283× 暖缓存，剩压测/CI benchstat）；M6.5 CI 接线（仓库无 .github/workflows，本地无 act/gh 难真验）；M6.1 维度续扩（角色/数据策略=per-app 需决策、用量事件计量）。
